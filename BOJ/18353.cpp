#include<iostream>
#include<cstdlib>
#include<algorithm>
#include<vector>
#include<cstring>
#include<string>
#define fore(i, n) for (int i = 0; i != (n); ++i)
#define FAST cin.sync_with_stdio(false); cin.tie(nullptr);
#define endl '\n'
using namespace std;
int tmp1, tmp2, tmp3, tmp4, tmp5;
int N, M, K, X, Y, Q, P, S, R, A, B, C;

int L[2020];

int main() { FAST
    cin >> N;
    fore (i, N) {
        cin >> X;
        if (tmp2 == 0 || L[tmp2-1] < X) L[tmp2] = X;
        else L[lower_bound(L, L + N, )]
    }
}
